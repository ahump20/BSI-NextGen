/**
 * Application Constants
 * 
 * Centralized configuration values used across the application.
 * These values should be imported rather than hardcoded to ensure
 * consistency and ease of maintenance.
 */

/**
 * Application version
 * Should match the version in package.json
 */
export const APP_VERSION = '1.0.0';

/**
 * Application timezone
 * All timestamps and dates are normalized to America/Chicago timezone
 */
export const APP_TIMEZONE = 'America/Chicago';
