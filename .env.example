# Environment Variables Example
# Copy this file to .env and fill in the actual values
# DO NOT commit .env to version control

# ============================================
# Deployment Platform Settings
# ============================================

# Node environment (development, production, test)
NODE_ENV=production

# CI environment (set to empty string to prevent warnings from being treated as errors)
# CI=

# ============================================
# API Keys and Secrets
# ============================================

# OpenAI API Key (example - replace with your actual service keys)
# OPENAI_API_KEY=your_openai_api_key_here

# Supabase Configuration (example)
# NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Database URL (example)
# DATABASE_URL=postgresql://user:password@host:port/database

# ============================================
# Build Configuration
# ============================================

# NPM flags for handling peer dependencies
# NPM_FLAGS=--legacy-peer-deps

# ============================================
# Application Settings
# ============================================

# Public URL/Domain
# NEXT_PUBLIC_APP_URL=https://your-app-url.com

# Analytics and Tracking IDs (example)
# NEXT_PUBLIC_GA_ID=your_google_analytics_id_here

# ============================================
# Feature Flags (example)
# ============================================

# NEXT_PUBLIC_ENABLE_FEATURE_X=true

# ============================================
# Important Notes for Netlify/Vercel
# ============================================

# 1. Variable names must EXACTLY match between this file and your deployment platform
#    - Netlify: Set in Site settings > Environment variables
#    - Vercel: Set in Project Settings > Environment Variables
#
# 2. Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
#    - Never put sensitive data in NEXT_PUBLIC_ variables
#
# 3. For Vite projects, use VITE_ prefix instead of NEXT_PUBLIC_
#    - Example: VITE_API_URL instead of NEXT_PUBLIC_API_URL
#
# 4. Always verify variable names match exactly (case-sensitive)
#    - OPENAI_API_KEY ≠ VITE_APP_OPENAI ≠ openai_api_key
#
# 5. Set variables for all deployment contexts:
#    - Production
#    - Preview (Deploy Previews / PR branches)
#    - Development
